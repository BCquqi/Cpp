#include<iostream>
using namespace std;

void f(string s) {
    int ans = 0;
    char op = 0;
    for (int i = 0;i < s.size();i++) {
        if (s[i] >= '0' && s[i] <= '9')
    }
}

int main() {
    string s;
    cin >> s;
    f(s);
    return 0;
}
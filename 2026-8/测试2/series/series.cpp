#include<iostream>
using namespace std;

int main() {
    int n,m;
    cin >> n >> m;
    while (m--) {
        int type;
        cin >> type;
        if (type == 1) {
            int l,r,t,p;
            cin >> l >> r >> t >> p;
        } else {
            int l,r;
            cin >> l >> r;
        }
    }
    return 0;
}